---
title: 'smoke test'
description: 'render check — math and code'
pubDate: 'Jun 06 2026'
---

인라인 수식 $O(n \log n)$ 과 블록 수식:

$$
T(n) = 2T(n/2) + O(n)
$$

코드 블록:

```python
def f(x):
    return x * 2
```
